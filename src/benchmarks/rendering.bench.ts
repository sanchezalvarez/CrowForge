import { describe, bench } from "vitest";

describe("Legacy Benchmark Placeholder", () => {
  bench("Noop benchmark to avoid Vitest error", () => {
    // This file is deprecated. See rendering.bench.tsx
  });
});
